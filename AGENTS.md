# تعليمات العمل على «قبل المشاهدة»

اقرأ `docs/PROJECT_STATE.md` ثم نفّذ بندًا واحدًا من `docs/ROADMAP.md` في كل جلسة.

الأولوية دائمًا للمسار الحرج: صحة الإنچين، استقلال المصادر، المعاملات الذرّية، تاريخ التدقيق، والاختبارات. لا تستهلك جلسة قوية في تلميع CSS أو نصوص إلا إذا كان ذلك يمنع تشغيل المسار الحالي. البنود الموسومة **خفيف / مجاني** مؤجلة عمدًا للخطة المجانية.

## ثوابت لا تتغير بلا قرار صريح

- الواجهة عربية و`RTL`، واللغة بسيطة ومصرية مفهومة.
- الإحساس دافئ وهادئ وعائلي: كريمي + أخضر غابي + برتقالي `#D9683B`.
- وضوح القراءة أهم من الزخرفة؛ لا تستخدم blur خلف النصوص أو أثناء حركتها.
- حركة شارة «تمت مراجعة النسخة» تبقى خفيفة وحادة. حرّك موضعها (`top`/`margin`) ولا تستخدم fractional `transform` للنص.
- لا تزدحم الواجهة ببطاقات عائمة كثيرة.
- كل تفاعل أساسي يجب أن يعمل بالكيبورد واللمس، مع `aria-label` عند الحاجة.
- لا تنشر أحكامًا عن أعمال حقيقية من غير بيانات مراجعة موثقة. بيانات النموذج يجب أن تبقى موسومة بأنها توضيحية.
- لا يُحوّل غياب البيانات إلى تخمين أو نتيجة آمنة.

## قبل إنهاء أي مهمة

```bash
npm run lint:local
npm run build:local
```

ولو لم تكن المهمة واجهة فقط، شغّل أيضًا:

```bash
npm run test:engine
npm run test:migrations
```

اختبر المقاسين: كمبيوتر وموبايل. حدّث `docs/PROJECT_STATE.md` لو تغيرت الحالة أو القرارات.
